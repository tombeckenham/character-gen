{
  "name": "character-gen",
  "private": true,
  "workspaces": [
    "packages/*",
    "gallery-app"
  ],
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p . && tsc --noEmit -p gallery-app",
    "build:gallery": "bun run --cwd gallery-app build",
    "lint": "oxlint .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "test": "node --test \"packages/*/src/**/*.test.ts\"",
    "check": "bun run typecheck && bun run lint && bun run format:check"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "lefthook": "^2.1.10",
    "oxfmt": "^0.59.0",
    "oxlint": "^1.74.0",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=22.13"
  }
}
